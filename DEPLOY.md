# نشر SoundInk (APVA Next) على سيرفر

## المتطلبات على السيرفر
- Docker + Docker Compose مثبّتين.
- ذاكرة (RAM) 4GB فأكثر — نموذج XTTS يحتاج ذاكرة كافية وقت التحميل والتوليد.
- مساحة قرص لا تقل عن 10GB (حجم النموذج + الحاويات).
- لو السيرفر بدون GPU، التوليد بيشتغل على المعالج (CPU) — أبطأ من GPU لكنه يشتغل.
  (`requirements.txt` فيه نسخة PyTorch الافتراضية اللي تدعم CUDA أيضًا — لو
  متأكد إن السيرفر بدون GPU وتبي تقلل حجم التحميل، بدّل خطوة تثبيت PyTorch
  بـ`pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu`
  قبل باقي المتطلبات.)

## خطوات النشر

1. **انسخ المشروع للسيرفر** (git clone أو رفع الملفات مباشرة). تأكد إن مجلدات
   `data/ref/` (الأصوات المرجعية) و`data/lexicon/` منسوخة معاك — هذي بيانات
   شخصية مو موجودة بالكود.

2. **جهّز ملف البيئة**:
   ```bash
   cp .env.example .env
   ```
   افتح `.env` وعبّي:
   - `API_KEY` — قيمة عشوائية قوية (مثلاً `openssl rand -hex 32`) لحماية الـAPI من الاستخدام العام.
   - `CORS_ORIGINS` — لو بتربطه بموقع خارجي، حط دومين الموقع بدل `*`.

3. **ابنِ وشغّل الحاوية**:
   ```bash
   docker compose up -d --build
   ```
   أول تشغيل بيحمّل نموذج XTTS (~2GB تقريبًا) — قد يأخذ عدة دقائق حسب سرعة الإنترنت.
   بعدها يُخزَّن بـ`model_cache` volume فلا يُعاد تحميله بكل إعادة تشغيل.

4. **تأكد إنه شغّال**:
   ```bash
   curl http://localhost:5050/health
   ```

5. **افتح المنفذ للوصول من جوالك/الإنترنت**:
   - لو تبي وصول مباشر: افتح المنفذ 5050 بجدار الحماية (firewall) الخاص بالسيرفر.
   - **الأفضل**: حط reverse proxy أمامه (Caddy أو Nginx) بشهادة SSL (HTTPS)،
     خصوصًا إذا رح تستخدمه من جوالك عبر الإنترنت — بدون HTTPS بيانات الطلبات
     (بما فيها API_KEY) تنمر بنص واضح.

## ⚠️ تنبيه أمني مهم
مفتاح `API_KEY` يحمي نقاط الـAPI، لكنه **حماية أساسية فقط**. لو رح تفتح
الخدمة على الإنترنت العام (مو بس شبكة محلية)، يُنصح بشدة تضيف طبقة حماية
إضافية أمام الخدمة كاملة:
- HTTP Basic Auth على مستوى الـreverse proxy، أو
- تقييد الوصول بعناوين IP محددة، أو
- ربطها بشبكة VPN خاصة (Tailscale مثلاً — سهل وسريع الإعداد).

بدون هذا، أي شخص يعرف عنوان السيرفر يقدر يجرّب يطلب توليد أصوات (استهلاك
معالجة/تكلفة) حتى لو ما يعرف الـAPI_KEY، لأن endpoint الواجهة الرئيسية `/`
نفسه غير محمي بمفتاح.

## التحديث لاحقًا
```bash
git pull
docker compose up -d --build
```

## عرض السجلات (logs)
```bash
docker compose logs -f soundink
```
